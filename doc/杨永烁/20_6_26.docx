  	根据爬取的微博内容（乘风破浪的姐姐）进行分词操作，并进行词频统计，生成词云。
	由于自然语言处理主体为中文，未使用NLTK库，使用jieba库；并使用wordcloud进行词云生成，辅以matplotlib、PIL库。
	读取文件类型为txt，分词后观察发现部分人名会被分词，需要人工添加分词库，不误分人名；
  	同时考虑到明星存在昵称、外号等，并不完全以本名出现在文本中，故需要将同义词进行整合，计算为统一词频；
  	实现将无意义词语汇总到一个txt，实现对无意义词语的删除，保证词频统计信息的有效性。
  	最终学习wordcloud库，实现绘制词云图。
